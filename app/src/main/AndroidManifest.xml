<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.patitas">

    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppThemeLight">

        <activity
            android:name=".util.SplashActivity"
            android:screenOrientation="portrait"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".pets.PetsActivity"
            android:screenOrientation="portrait"/>

        <activity
            android:name=".MainActivity"
            android:screenOrientation="portrait" />

        <activity
            android:name=".petcreator.PetCreatorActivity"
            android:parentActivityName=".pets.PetsActivity"
            android:screenOrientation="portrait" />

        <activity
            android:name=".petdetail.PetDetailActivity"
            android:parentActivityName=".pets.PetsActivity"
            android:screenOrientation="portrait" />

        <activity
            android:name=".auth.AuthActivity"
            android:parentActivityName=".pets.PetsActivity"
            android:screenOrientation="portrait" />

    </application>

</manifest>